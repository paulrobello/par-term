# Files allowed past the 800-production-line threshold that
# `scripts/check_line_counts.py` enforces. One `path: reason` per line; the
# reason is required. An entry that is no longer needed is reported by the
# script but never fails it, so removing a violation never turns CI red.
#
# An exemption is a debt, not a verdict. Everything here except the generated
# table is expected to leave this file.

# Static `BOX_DRAWING_ENTRIES` glyph geometry table. One entry per box-drawing
# codepoint; there is no seam to split on and splitting it would only scatter
# the table. Permanent.
par-term-render/src/cell_renderer/block_chars/box_drawing_data.rs: generated-style static glyph geometry table, no natural seam

# The three below are real violations, owned by other in-flight work. They are
# listed so the gate can land green and start protecting every other file
# rather than waiting on three unrelated refactors.
#
# Reasons deliberately carry no line numbers. A hand-written count here would
# drift exactly the way the five ARC-009 header comments did; the script prints
# the live count next to each entry instead.
par-term-config/src/config/config_struct/mod.rs: drained by the ENH-007 Config decomposition, not an independent split
par-term-config/src/snippets.rs: ENH-009 step 3, sequenced with the QA-037 git-rev-parse timeout fix
src/app/triggers/mod.rs: ENH-009 step 4, sequenced after SEC-002 stops reusing its security stack
